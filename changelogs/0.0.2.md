## Changelog for release 0.0.2

Release 0.0.2 addresses the following changes:

- Fixed issue where the access to any law-book was not restricted
- Added field `isMemberOf` to `LawBookResponse` for rest-schema. Is set to true if the user requesting a book is a
  member of that book, false when the user only has access to that book via an invitation
